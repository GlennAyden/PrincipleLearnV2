# API Reference (moved)

This file has been consolidated into [`API_REFERENCE.md`](API_REFERENCE.md).
All endpoint documentation, schemas, auth/CSRF/rate-limit conventions, and the
schema index now live there. This stub remains only to keep existing links
from breaking.
